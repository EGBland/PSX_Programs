#ifndef __MNCETC_H_DEFINED
#define __MNCETC_H_DEFINED 1

#include <stdio.h>

int min(int a, int b);
int max(int a, int b);

void int_to_bin_str(char *, int);
void short_to_bin_str(char *, short);

void char_to_hex_str(char *, char);

#endif